## 2026-01-09
- 目的: 編集画面（SlideEditor）の上部バーを「主役2つ（プレビュー/書き出し）」に絞り、その他は「ファイル/編集」メニューへ退避して視線負荷を下げる。

### 変更
- `components/SlideEditor.tsx`:
  - 上部バー左側に「ファイル」「編集」メニューを追加。
  - 既存の「読込/保存/名前をつけて保存/プロジェクト管理」「Undo/Redo」をメニュー内へ移動。
  - 「プレビュー」「書き出し」は右側の大きめボタンとしてそのまま主役で表示。
  - メニューは外側クリック/ESCで閉じる。

### 検証
- `npm test` PASS（210 tests）
