## 2025-12-17
- 不具合: スライド一覧のサムネで、背景（canvas）に画像を追加しても黒が残り、背景画像が見えにくい/見えない。
- 原因: canvas 追加の画像が初期サイズ 0.3 のままで、スライドに隠れたり余白(黒)を覆えない。
- 対応: `components/SlideInspector.tsx` の画像追加（canvasモード）をデフォルトで「cover」相当にし、背景画像がフレームを埋める初期サイズになるよう変更。
- 検証: `npm test` / `npm run build` 成功。