# 進捗: デフォルト背景画像を軽量化（2026-01-02）

## 背景
- カバーフロー操作中の「画面全体が一瞬フラッシュ」問題の切り分けで、
  - 背景を単色にすると減る
  - blur（backdrop-filter）を0にすると減る
  → 「重い背景画像 × blur の再合成」が主因っぽいと判明。

## 変更内容（最小）
- 高解像度の `Doc/IMG_9349.PNG`（約32MB / 5504×3072）を、背景用に軽量化したWebPを追加。
  - 追加: `Doc/IMG_9349_BG.webp`（1920×1072 / 約322KB）
- デフォルト背景参照を軽量版へ差し替え。
  - `utils/glassPrefs.ts` の `DEFAULT_IDLE_BG_IMAGE_URL`
  - `index.css` の `--idle-bg-image`

## ねらい
- 背景のデコード/合成負荷を下げて、カバーフローのスクロール/3D変形中でもフラッシュ（チラつき）を出にくくする。

## テスト
- `npm test` PASS（173 tests）。
