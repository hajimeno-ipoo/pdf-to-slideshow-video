- ガラス設定モーダルに「左プレビュー」を追加し、背景/ガラスの見え方をその場で確認できるようにした（プレビューはmd以上でsticky）。
- プレビュー操作（画像モードの自由/タイル時）:
  - ドラッグで位置移動（タイルはずれ）
  - ホイール/ピンチでズーム（scale 50-200）
  - ダブルクリックで位置/ズームを初期化
- セクション別リセットを追加:
  - 背景: backgroundMode/色/表示/scale/位置をデフォルトへ（画像データ自体は消さない）
  - ガラス: tint/透明度/blurをデフォルトへ
- UI文言: タイル時は「タイルのずれ（左右/上下）」に変更。
- 検証: `npm test` PASS（120 tests）、`npm run build` PASS。