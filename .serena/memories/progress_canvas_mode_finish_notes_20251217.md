## 2025-12-17
- キャンバス編集（スライド位置/サイズ自由＋背景オブジェクト配置＋レイヤー順）はコード側の反映とビルド/テストを通過。
- この実行環境では Vite dev server が `listen EPERM` で起動できず（127.0.0.1:5173 でも不可）、Playwrightでの実機UI操作確認はできなかった。
- 代替として `npm test` / `npm run build` を再実行し、成功を確認。
- キャンバス編集の視覚二重表示対策として、スライド枠の背景は `overviewImage` を crop 表示する方式に統一済み。