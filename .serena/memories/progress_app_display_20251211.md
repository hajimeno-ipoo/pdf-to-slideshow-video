## 2025-12-11
- 問題: アプリが表示されないとの報告。調査でビルド・プレビュー自体は表示されるが、ビルド成果物を file:// で直接開くとモジュール読み込みが CORS でブロックされる可能性を確認。
- 対応: Vite の base を './' に変更してサブパス配信・静的ホスティング時の 404 空白を防止 (vite.config.ts)。
- テスト: npm run build 成功。file:// 直開きはブラウザ制約でブロックされることを確認。プレビュー/開発サーバー経由では表示を確認済み。
- 次回: 表示されない場合は必ず dev/preview 等のローカルサーバー経由で開くよう案内し、必要なら CORS を許可する静的ホストを用いる。