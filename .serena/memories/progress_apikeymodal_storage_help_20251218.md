## 2025-12-18
- 目的: APIキー設定モーダルで「保存先（メモリのみ/このタブだけ/この端末に保存）」の違いをヘルプ表示したい。
- 対応: `components/ApiKeyModal.tsx` にヘルプボタンと説明ボックスを追加。
  - ヘルプは「保存先」見出し右のボタンで開閉。
  - モーダルを開き直したときはヘルプを閉じた状態にリセット。
- 説明内容: メモリのみ/このタブだけ/この端末に保存の動作＋保存先変更時は選んだ先だけに保存（他は消す）を明記。
- 検証: `npm test` 成功、`npm run build` 成功（viteの一時ファイル書き込みがあるため権限が必要な環境あり）。