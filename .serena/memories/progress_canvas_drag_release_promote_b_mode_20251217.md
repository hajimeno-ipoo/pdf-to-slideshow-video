## 2025-12-17
- ユーザー要望(B): ちょっとはみ出した瞬間に `space:'canvas'` が確定するのは違和感 → **ドラッグ中は見た目だけスライド外に見せて、マウスアップ（ドロップ）で確定**に変更。

### 修正
- `components/SlideInspector.tsx`
  - ドラッグ移動中の自動 `space:'canvas'` 変換を削除（移動中は `x/y` だけ更新）。
  - ドラッグ中に限り、選択中の slide-space オーバーレイを **キャンバス座標に換算した“プレビュー”として stage 最前面に描画**し、スライド内描画（クリップ）からは一時的に除外。
  - マウスアップ時に、最終 `x/y` がスライド範囲外なら `space:'canvas'` へ昇格＋座標/サイズ/見た目スケールを換算して確定。

### 検証
- `npm test` / `npm run build` 成功。