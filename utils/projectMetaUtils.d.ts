import type { ProjectData } from '../types';

export declare const estimateProjectBytes: (projectData: ProjectData | null | undefined) => number;
export declare const formatBytes: (bytes: number) => string;

